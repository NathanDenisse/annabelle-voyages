rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Lecture publique de tous les fichiers
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
